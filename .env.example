# Cloudflare named-tunnel token.
# Get it in the Cloudflare Zero Trust dashboard:
#   Networks > Tunnels > (create/select your tunnel) > copy the token.
# Then: copy this file to ".env" and paste the token below.
# ".env" is git-ignored -- never commit your token.
TUNNEL_TOKEN=
